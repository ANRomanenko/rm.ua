Git команды:
1. Что бы инициализировать новый проэкт Git нужно использовать команду "git init"
это инициализация git в нашем проэкте.

2. Настойки git
Настройка пользователя git config --global user.name "Romanenko"
Настройка эмейл git config --global user.email "test@gmail.com"

3. Проверка статуса нашего проэкта git status

4. Для того что бы git мог отслеживать опредеденные файлы мы можем использовать
2 способа:
1-вый способ git add pogoda.php "Это конкретный файл"
2-ой способ git add -all для добавления всех изменённых файлов

5.
